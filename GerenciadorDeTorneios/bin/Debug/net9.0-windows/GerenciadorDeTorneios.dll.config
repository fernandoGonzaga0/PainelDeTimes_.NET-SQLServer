<?xml version="1.0" encoding="utf-8" ?>

<!--Esse arquivo serve como arquivo de configuração, usado para connection string (que faremos para vincular ao banco de dados no SQL Server Management)-->

<configuration>

	<!--ConnectionStrings é uma seção especial do App.Config onde definimos as strings de conexão com bandos de dados.
		É basicamente um texto que contém todas as informações necessárias para o .NET se conectar ao banco de dados.-->
	
	<connectionStrings>
		<!--
		
		name -> nome da conexão que estamos buscando
		connectionString -> texto que concatena o servidor, o banco de dados do servidor e se a conexão é confiável 
		providerName -> provedor que o .NET usa para conectar ao bds (para SQL Server sempre será System.Data.SqlClient)
		
		-->
			
		<!--É necessário inserir Trusted_Connection, Encrypt e TrustServerCertificate para conectar ao banco sem certificado SSL-->
		<add 
			 name="GerenciadorDeTorneios" 
			 connectionString="Server=DESKTOP-SA0CEQA;Database=GerenciadorDeTorneios;Trusted_Connection=True;Encrypt=True;TrustServerCertificate=True"
			 providerName="System.Data.SqlClient"
		/>
		
	</connectionStrings>
	
</configuration>